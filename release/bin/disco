#!/bin/bash
#
# Usage: disco [ <filename> ]
#
# Runs the given script file and compares resulting temporary log with
# corresponding test log file <filename>.test, if it exists
#
dir="`dirname $0`"
"$dir/_run" edu.wpi.disco.Disco "$1" lib/disco.jar
