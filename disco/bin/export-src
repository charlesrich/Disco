#!/bin/bash
# 
# Usage: export-src [ <path> ]

# Create src.zip with disco and task sources in given path (defaults to /tmp)
#
cd "`dirname $0`/../.."
cd disco/java
zip -qr /tmp/src.zip .
cd ../../task/java
zip -qrg /tmp/src.zip .
if [ "$1" ]; then
  cp -v /tmp/src.zip $1
fi
